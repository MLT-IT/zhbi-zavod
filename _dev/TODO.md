* Файловые сниппеты, вызываемые через Fenom, не кешируются.
Если их кешировать, то можно ускорить сайт.


* На странице избранного в кровле пустой споилер с характеристиками на мобилках. Надо бы это предусмотреть.


* Обертка для характеристик (listing__products-item-chars-wrap) видна, даже если характеристик нет — из-за отступа. Смотри в кровле.


* Возможно, если на странице каталога сделать поменьше элементов в блоке mse2_mfilter, то данных с сервера будет приходить меньше, и это ускорит ajax.


* Кажется, в некоторых местах есть несколько аналогичных if / switch. Например, для логотипа, баннера, favicon. Я думаю, что лучше задать переменную. И потом просто подставлять ее. Сайт быстрее будет работать.


* Вместо include в некоторых случаях можно использовать insert. Быстрее будет работать.


* Хорошо бы на сайте найти все теги `<a>`, которые выполняют роль кнопок (на них e.preventDefault). И заменить на некликабельный тег. Потому что при загрузке страницы можно нажать на этот тег, а скрипты еще не успели подгрузиться. Будет перезагрузка страницы. Это нехорошо.


* В микроразметке есть brand. Но он ведь только у утеплителей. Может, убрать его?


* Возможно, на сайте есть мусор. Надо написать скрипт, который от первого id до самого последнего по порядку проходит и ищет товары и связи / картинки. Если товара нет, а связи / картинки есть, то удалить их.
Чем меньше записей в БД, тем быстрее в ней осуществляется поиск. Мусорные записи могут тормозить сайт. А картинки занимают место на хостинге.


* На сайте есть много картинок с заглушкой "Нет фото". Они импортировались как настоящие картинки. И весят также. Можно их всех убрать, заглушка на сайте от phpthumb и так есть. И весит всего как 1 картинка.


* Раньше У msProducts не было параметра returnIds, как у pdoResources. Возможно, где-то я вызываю msProducts только для получения id. А происходит выборка всех полей из БД. Это гораздо медленнее, чем выбрать просто id. Надо просмотреть все сниппеты. И если это где-то есть, то подправить.


* Пережать картинки всех товаров через XnConvert.
P.S. Я пережал картинки. id последнего товара: 77202.


* Пережать все документы, которые возможно. Там не только pdf, но и картинки есть. Их точно можно пережать.


* По 2 класса для избранного и сравнения. Надо свести к одному, код будет чище и проще.


* Подправить ошибки валидации. 


* Хорошо бы все сниппеты, которые я писал, сделать статичными. Чтобы можно было искать их код через IDE (глобальный поиск), чтобы была лучше подсветка синтаксиса, чтобы удобнее было работать, чтобы они были в GIT.


* На новой карточке для кровли (карточка с перелинковкой) есть небольшой баг. Если добавить товар в избранное на мобильной версии, а потом развернуть версию для ПК, кнопка будет такой, будто товар не добавляли в избранное. То же самое со сравнением.


* В разных контекстах некоторые ресурсы называюся разно. Это может вызвать баг, я ведь на сайте пишу относительные ссылки. Надо написать скрипт, который проверит все alias'ы. И выдаст, где есть несоответствие. Изменить это несоответствие. Поставить редиректы.


* svg это и шрифты, и картинки. Надо написать регулярку для разграничения - в `webpack.config.js`.


* map-scroll-cover не меняется при изменении ширины окна. Это не очень хорошо. Кажется, это несложно подправить.


* На страницах много ненужных оберток. Например, много `.wrapper`. Можно избавиться от них.


* Это одно и то же, меняется только заголовок. Хорошо бы менять его через JS, чем подключать 3 почти одинаковых чанка.
    popup-cost-fanera.tpl
    popup-callback.tpl
    popup-cost-delivery.tpl


* На карточке товара для фанеры и плит ОСБ расчеты происходят при загрузке страницы через JS. Хорошо бы через fenom это сделать.


* custom-counter_type_fractional - мне кажется, что лучше добавить этот класс непосредственно для input, а не для обертки. Как data-min.


* Я заметил один баг. Если у товара разрешено дробное добавление кол-ва. При концертации оно обновляется до целого. Но добавляется то дробное! Вверху справа написано в зеленом сообщении дробное, а в счетчике на товаре целое.


* Слайдер с производителями очень плохо работает. Попробуй посжимай экран и понажимай кнопки слайдера.


* В миникорзине (которая появляется при наведении) Итого: 23856 руб. Меняешь кол-во товара и ставится пробел. Хорошо бы сразу с пробелом выводить.


* В сниппетах для перелинковки $idsInParent используется pdoResources, а должен msProducts
  А еще, кажется, можно сделать еще точнее выборку. 


* Предлагаю написать код, который ночью или вообще каждый час открывает страницу Каталог у каждого контекста. Т.к. это самые медленные страницы. При сбросе кеша они открываются очень медленно. А cron позволит генерировать кеш.
Или даже после очистки кеша.


* Форма из миникорзины ТОЖЕ ДЕЛАЕТ РЕДИРЕКТ. Это не надо.


* Почему pilomat_thing - это не thing? Зачем второе свойство? Наверно, когда-то не было в коде if'ов для проверки на контекст. Поэтому пришлось еще ключ делать. А сейчас можно убрать.


* Предлагаю на мобилках убрать скроллбары из фильтров. Там просто будет один большой скроллбар, который скроллит весь этот выдвигающийся сайдбар.


* При загрузке фильтров сейчас я просто добавляю ::after с прозрачным блоком, чтобы нельзя было выбрать другой фильтр. Но можно написать лучше - подредактировать стандартный JS файл для mSearch2:
```
    beforeLoad: function () {
        $(this.options['wrapper']).addClass(this.options['loading_class']);
        this.results.css('opacity', .5);
        $(this.options.pagination_link).addClass(this.options.active_class);
        this.filters.find('input, select').prop('readonly', true).addClass(this.options.disabled_class);
    },
```
Тогда можно при наведении на чекбокс изменить курсор на какой-нибудь другой.
Но стоит ли оно того? Тем более, что чекбоксы у нас обернуты в тег a. То есть это не совсем простая задача, как мне кажется.

* Предлагаю сделать так, что если ед. измерения всего 1, то не будет списка с выбором. А сейчас он может быть.

* Кнопка "Подняться вверх" видна всегда. Хорошо бы сделать так, чтобы она появлялась, когда проскроллили вниз немного. 

* На некоторых ссылках нет hover'ов. Предлагаю их добавить.
  Например, кнопка "Заказать звонок" в шапке. Фильтры.

* Я так и не дописал скрипт для правильного получения отзывов. Там плохо считается вложенность. Мы отложили на потом, но так и не вернулись к нему.

* Сделай так, чтобы в слайдере у самого последнего товара всплывашка с кнопками разворачивалась не вправо, а влево.

* Если в ресурсе поменять значение (h1 или содержимое), то сломается замена на UTM, потому что для замены используется replace. Это ненадежно.

* Кирилл сказал уменьшать h1, если он длинный. Я уменьшаю при загрузке страницы. Но в категориях и каталоге h1 может меняться динамически.
Надо скрипт для уменьшения тоже сделать динамическим.
