Файл:
public_html\assets\components\mspre\js\mgr\misc\mspre.combo.select.js

После этих строк:
```
mspre.combo.FilterOptionsValues = function (config) {
    config = config || {}

    if (config.custm) {
        config = getDefaultConfig(config)
    }
```

Я добавил строку:
```
config.minChars = 1;
```
